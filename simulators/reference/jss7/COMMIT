Repository: RestComm/jss7
URL: https://github.com/RestComm/jss7
Branch: master
Commit pinned: 81a54df19bb24878ab21bb88377ca45533b3a974
Referenced: 2026-08-14, extended 2026-08-15

Real, cited, arms-length reference only -- this project's own source (libs/tcap-core, real ITU-T
Q.773 TCAP/dialogue-portion codec) cites specific real facts from jss7's own real source (tag
numbers, tag classes, ITU-T table numbers already quoted in jss7's own Javadoc, real wire-shape
behavior confirmed from its own real encode()/decode() logic) -- see the citing code's own header
comments (libs/tcap-core/include/tcap_core/component.hpp, message.hpp, dialogue_portion.hpp) for
exactly which facts and which real jss7 source files they came from.

NOT linked into this project's build: AGPL-3.0 (this repository's own real, authoritative LICENSE,
confirmed directly from GitHub, not from stale per-file LGPL-2.1 headers some individual jss7
source files still carry) is not compatible with linking into this project's own Apache-2.0-
licensed code -- see docs/DECISIONS.md's own Stage 5b ADR update for the full real license-check
evidence, the same conclusion already reached for Osmocom/SCCP in Stage 5a.

2026-08-15: the actual jss7 Java source files that were previously vendored into this directory
(arms-length reference copies, never linked into the build) have been removed from this
repository's own git history going forward -- real .java files sitting in a public repo, even as
disclosed non-linked reference material, made GitHub's own language detection report "Java" as a
language for this project, which is not accurate to what this project actually is or ships. Real
facts already extracted from those files remain cited in this project's own code (see above); if a
fact needs re-verification against jss7's own real source in a future session, fetch it fresh from
the pinned commit above (e.g. `gh api repos/RestComm/jss7/contents/<path>?ref=<commit>`) rather
than re-vendoring it into this repository.
