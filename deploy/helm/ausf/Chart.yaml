apiVersion: v2
name: ausf
description: >
  AUSF (Authentication Server Function) -- TS 29.509 Nausf_UEAuthentication ue-authentications
  group (5G-AKA and EAP-AKA' for a normal UE). Lab-grade Helm chart; see docs/DECISIONS.md for
  what "lab-grade" means here (in-memory auth-context storage, no HA). DISCLOSED GAP, not
  silently shipped: same as deploy/helm/udr/Chart.yaml -- this chart does not provision PKI at
  all, and ausf/udr/udm/smf/amf/nrf are separate Helm releases with no shared-secret mechanism
  between them yet. Proper fix deferred to Phase 8 (lab packaging and conformance) per PROMPT.md.
type: application
version: 0.1.0
appVersion: "0.1.0"
